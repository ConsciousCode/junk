#ifndef DEF_HPP
#define DEF_HPP

/**
 * The size (in blocks) to chunk as a single mesh.
**/
#define CHUNK_SIZE 16

/**
 * The smallest a block can get, 2^-MAX_SUBBLOCK meters
**/
#define MAX_SUBBLOCK 4

/**
 * The id of air/nothingness.
**/
#define NOTHING 0

#endif
